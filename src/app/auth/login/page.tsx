import { Center } from "@chakra-ui/react";
import LoginForm from "./_components/loginForm";

const Page = async () => {
  return (
    <Center>
      <LoginForm />
    </Center>
  );
};

export default Page;
