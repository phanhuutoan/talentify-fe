import { Center } from "@chakra-ui/react";
import { MainProfile } from "./_components/MainProfile";

const Page = async () => {
  return (
    <Center>
      <MainProfile />
    </Center>
  );
};

export default Page;
